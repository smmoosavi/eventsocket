var expect = require('chai').expect;
//var EventSocket = require('../lib/EventSocket');

describe('eventSocket', function () {
    it('nope', function () {
        expect(1).to.be.equals(1);
    });
    it('coming...', function () {
        expect(1).to.be.equals(1);
    });
});